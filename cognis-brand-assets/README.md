# Cognis Knowledge — RAGFlow brand assets

RAGFlow ships a React web UI. The Cognis layer rebrands it via env-driven config in `api/cognis/branding.py` + a theme overlay in `web/src/branding/` (TBD — v1 ships text-level rebrand via `COGNIS_PRODUCT_NAME` env; full visual rebrand including logo SVG is a v1.5 follow-up once design lands).

Reserved for SVG assets when design is ready.
